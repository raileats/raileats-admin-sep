/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // appDir is enabled by default in modern Next.js; don't use experimental.appDir
  // Keep other settings minimal — you can extend as needed
};

module.exports = nextConfig;
